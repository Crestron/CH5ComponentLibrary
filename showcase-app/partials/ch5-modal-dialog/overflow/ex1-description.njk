<h1>attribute: overflow</h1>
<p>
    Valid values: 'scroll' and 'show'.
</p>
<p>
    The overflow property specifies what happens if content does or does not overflow the component box.
    If 'scroll' is selected, the scrollbars might appear if content is larger than the available area.
    If 'show' is selected, the overflow is not clipped.
</p>
<p>The following example is for overflow='scroll':</p>